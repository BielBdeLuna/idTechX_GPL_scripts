//this file is GPLv3

// depending on whether if it is actually a limit or a threshold, it checks if a value surpases a threshold or not.

boolean threshold ( boolean limit, float value, float thold ) {
    if ( value >= thold ) {
        if ( limit ) {
            return false; //outside the limit or in it.
        } else {
            return true; //we've reached the threshold or surpassed it.
        }
    }

    if ( limit ) {
        return true;    //within the limit.
    } else {
        return false;   //not yet reached the threshold.
    }
}

// invert a boolean, I wonder if this can be achieved by negating the boolean in an equation " = !element "
boolean invertBool ( boolean element ) {
    if ( element ) {
        return false;
    } else {
        return true;
    }
}
